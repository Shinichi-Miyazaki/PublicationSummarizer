# 限定公開用パスワードの設定例。
#
# ローカルで使う場合: このファイルを同じフォルダに「secrets.toml」という名前でコピーし、
#   値を変更してください（secrets.toml は Git にコミットされません）。
# Streamlit Community Cloud の場合: アプリの Settings → Secrets に下記を貼り付けます。
#
# パスワードを設定しない（＝誰でも閲覧可）場合は、この行を消すか secrets.toml を作らないでください。

app_password = "ここに研究室の共有パスワード"
